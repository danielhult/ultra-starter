"use client";

import Loader from "@/components/Loader";
import Hero from "@/components/Hero";

const Home = () => {
  return (
    <main>
      <Hero />
      <Loader />
    </main>
  );
};

export default Home;
